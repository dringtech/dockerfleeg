diff --git a/config/boot.rb b/config/boot.rb
index 4489e58..f36d111 100644
--- a/config/boot.rb
+++ b/config/boot.rb
@@ -4,3 +4,6 @@ require 'rubygems'
 ENV['BUNDLE_GEMFILE'] ||= File.expand_path('../../Gemfile', __FILE__)
 
 require 'bundler/setup' if File.exists?(ENV['BUNDLE_GEMFILE'])
+
+# Fix to resolver issue
+require 'resolv-replace'
diff --git a/config/mongoid.yml b/config/mongoid.yml
index f127da8..dc8eb10 100644
--- a/config/mongoid.yml
+++ b/config/mongoid.yml
@@ -1,6 +1,6 @@
 # This file is overwritten by one in alphagov-deployment at deploy time
 development:
-  host: localhost
+  host: mongo
   database: govuk_content_development
   persist_in_safe_mode: true
   use_activesupport_time_zone: true
